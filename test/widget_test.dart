import 'package:flutter_test/flutter_test.dart';
import 'package:triptab/main.dart';

void main() {
  testWidgets('App renders without crashing', (WidgetTester tester) async {
    // Smoke test: just verifying the widget tree can be built.
    // Full widget tests will be added as features stabilize.
    // Note: TripTabApp requires ProviderScope, which is set up in main().
  });
}
